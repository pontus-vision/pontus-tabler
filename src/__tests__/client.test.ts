import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { getContentModel } from "../client";

describe('Testing getContentModel',() => {
  it('gets ref-inputs properly', ()=>{
    const mock = vi.fn()
  })
})


