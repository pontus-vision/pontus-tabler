name: api
error-discrimination:
  strategy: status-code
display-name: Simple Inventory API
environments:
  Default: https://virtserver.swaggerhub.com
default-environment: Default
auth-schemes:
  BearerAuthScheme:
    scheme: bearer
auth: BearerAuthScheme
